describe('NewUserFormView', function() {
  var view;

  beforeEach(function() {
    appendLoadFixtures("user_form.html");
    console.log(typeof NewUserFormView);
    console.log(typeof NavigationView);

    // view = new NewUserFormView();
  });

  // it('exists', function() {
  //   expect(view).toBeTruthy();
  // });
});